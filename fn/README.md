# Fn

Fn was generated using Turborepo. All of this should be run inside a docker container. ie. `docker-compose run --rm fn dev`

### Build

To build all apps and packages, run the following command:

```
cd fn
pnpm build
```

### Develop

To develop all apps and packages, run the following command:

```
cd fn
pnpm dev
```
