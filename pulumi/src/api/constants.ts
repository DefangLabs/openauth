import * as path from "path";
import { PROJECT_ROOT_PATH } from "../common/constants";

export const SERVICE_NAME = 'api';

export const DIRNAME = 'fn';

export const SERVICE_ROOT_PATH = path.resolve(PROJECT_ROOT_PATH, DIRNAME);

